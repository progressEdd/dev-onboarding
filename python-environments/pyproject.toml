[project]
name = "dev-env-py"
version = "0.1.0"
description = "example project to getting poetry setup within a conda environment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=6.29.5",
]
